"""Fake provider server — implements Slack + Jira surfaces for offline testing.

Runs on :7704. The entire integration suite passes against these fakes,
so development stays offline and CI needs no secrets.
"""
